# Compatibilidade com hosts externos que usam npm (readdy.ai, Vercel, Netlify)
# Os plugins opcionais do Manus declaram peer deps antigas de vite (^4||^5);
# legacy-peer-deps evita erro ERESOLVE no npm install. pnpm ignora este ajuste.
legacy-peer-deps=true
